local M = {}

M.setup = function()
	-- not needed (yet), but added a setup function here so it doesn't break if
	-- someone puts `opts = {}` or `config = true` in their lazy.nvim plugin spec
end

return M
